#include<stdio.h>
int main()
{   
        char aa;
        printf("Enter the character u want :");
        scanf("%c",&aa);
        
        printf("the char u inserted is %c",aa);
        return 0;
}
